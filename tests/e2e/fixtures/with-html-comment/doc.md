<!-- This comment should not be visible in the rendered preview. -->

# HTML Comment Fixture

Visible paragraph text used as an anchor for the fixture heading check.

<!-- This second standalone comment should also be stripped. -->

```html
<!-- This comment is INSIDE a fenced code block and must remain visible as literal text. -->
<div>example</div>
```

<div>This raw tag sits outside any fence and must still render as literal escaped text — it is not a comment, so the fix must not touch it.</div>
